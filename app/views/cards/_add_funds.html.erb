<%= form_with do |form| %>
  <div class="field add-funds-form">
    <br />
    <%= form.text_field :amount, class: 'small-form form-control' %>
    <div class="actions add-fund-button">
      <%= hidden_field_tag(:card_token, card['token']) %>
      <%= form.submit 'Go!' %>
    </div>
  </div>

<% end %>
